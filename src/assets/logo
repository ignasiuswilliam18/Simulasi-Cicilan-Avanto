import React from 'react';
import { CalculationResult } from '../../../Engine/financingEngine';

interface ResultProps {
  results: CalculationResult[];
  selectedProviderName: string;
}

export const Result: React.FC<ResultProps> = ({ results, selectedProviderName }) => {
  return (
    <div className="w-full bg-neutral-900 border border-neutral-800 p-6 rounded-2xl flex flex-col gap-6 mt-6">
      <div>
        <h2 className="text-xl font-bold text-white">Simulasi Hasil Cicilan</h2>
        <p className="text-sm text-neutral-400">
          Asumsi perhitungan menggunakan skema terendah dari <span className="text-emerald-400 font-semibold">{selectedProviderName}</span>
        </p>
      </div>

      <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-4">
        {results.map((item) => (
          <div
            key={item.tenor}
            className="bg-neutral-950 border border-neutral-800 hover:border-emerald-500/50 p-5 rounded-xl flex flex-col justify-between gap-4 transition-all"
          >
            <div>
              <div className="text-xs font-bold text-emerald-400 uppercase tracking-wider bg-emerald-500/10 px-2.5 py-1 rounded-md w-fit mb-3">
                Tenor {item.tenor} Bulan
              </div>
              <div className="text-2xl font-extrabold text-white tracking-tight">
                Rp {item.monthlyInstallment.toLocaleString('id-ID')}
                <span className="text-xs font-normal text-neutral-500 ml-1">/ bln</span>
              </div>
            </div>

            <div className="flex flex-col gap-1.5 text-xs text-neutral-400 border-t border-neutral-900 pt-3">
              <div className="flex justify-between">
                <span>Harga Pokok:</span>
                <span className="text-neutral-200">Rp {item.totalPrincipal.toLocaleString('id-ID')}</span>
              </div>
              <div className="flex justify-between">
                <span>Total Bunga:</span>
                <span className="text-neutral-200">Rp {item.totalInterest.toLocaleString('id-ID')}</span>
              </div>
              <div className="flex justify-between">
                <span>Biaya Admin:</span>
                <span className="text-neutral-200">Rp {item.totalAdminFee.toLocaleString('id-ID')}</span>
              </div>
              <div className="flex justify-between font-bold border-t border-neutral-900 pt-1.5 mt-1 text-white">
                <span>Total Bayar:</span>
                <span className="text-emerald-400">Rp {item.grandTotal.toLocaleString('id-ID')}</span>
              </div>
            </div>
          </div>
        ))}
      </div>
      
      <div className="text-center text-xs text-neutral-500 mt-2 bg-neutral-950/50 p-3 rounded-xl border border-neutral-900/50">
        *Harga yang muncul merupakan simulasi dengan asumsi Bunga dan Admin terendah. Lakukan Apply dan Ajukan Transaksi di Aplikasi Avanto untuk perhitungan detail.
      </div>
    </div>
  );
};

export default Result;
